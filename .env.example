# Copy to .env and fill in. .env is gitignored.

# Live (dynamic) testing — required only for the dynamic stage.
HARNESS_API_KEY=pat.xxxxxxxx.xxxxxxxx
HARNESS_ACCOUNT=your_account_id
HARNESS_BASE_URL=https://app.harness.io

# Semantic static checks — optional; without it LLM checks are skipped.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
